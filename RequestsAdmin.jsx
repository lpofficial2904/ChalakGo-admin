import { useEffect, useState } from 'react'
import './styles.css'
const api = async path => { const token = sessionStorage.getItem('chalakgo_admin_token'), r = await fetch('https://chalakgo.onrender.com' + path, { credentials: 'include', headers: token ? { Authorization: `Bearer ${token}` } : {} }), d = await r.json(); if (!r.ok) throw Error(d.message); return d }
export default function RequestsAdmin() { const [tab, setTab] = useState('bookings'), [items, setItems] = useState([]), [error, setError] = useState(''); useEffect(() => { api(`/api/${tab}/admin`).then(setItems).catch(e => setError(e.message)) }, [tab]); const title = tab === 'bookings' ? x => `${x.service} — ${x.fullName}` : tab === 'contacts' ? x => x.name : x => x.fullName; const sub = tab === 'bookings' ? x => `${x.phone} · ${x.pickupLocation}` : tab === 'contacts' ? x => `${x.phone} · ${x.email} · ${x.message}` : x => `${x.mobile} · Joined ${new Date(x.createdAt).toLocaleDateString()}`; return <main className="admin-loading"><section className="card editor" style={{ width: 'min(940px, calc(100vw - 32px))', margin: '32px auto' }}><div className="form-head"><div><small>CHALAKGO ADMIN</small><h2>Customer requests</h2><p>All bookings, contact enquiries and registered users.</p></div></div><div style={{ display: 'flex', gap: 8, marginTop: 20 }}><button className={tab === 'bookings' ? 'primary' : 'secondary'} onClick={() => setTab('bookings')}>Bookings</button><button className={tab === 'contacts' ? 'primary' : 'secondary'} onClick={() => setTab('contacts')}>Contacts</button><button className={tab === 'users' ? 'primary' : 'secondary'} onClick={() => setTab('users')}>Users</button></div>{error && <p className="empty">{error}</p>}<div className="rows">{items.map(x => <article className="row" key={x._id}><div className="copy"><b>{title(x)}</b><small>{sub(x)}</small></div><span className="status live">{x.status || 'Registered'}</span></article>)}{!items.length && !error && <p className="empty">No records yet.</p>}</div></section></main> }
